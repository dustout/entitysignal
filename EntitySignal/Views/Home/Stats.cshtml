@{
  ViewData["Title"] = "Statistics Page";
}

@{ 
  var connectedUsers = EntitySignal.Services.EntitySignalDataStore.SubscriptionsByType.SelectMany(x=>x.Value.SubscriptionsByUser.Select(y=>y.Key)).Distinct();
}

<div class="row">
  <div class="col">
    <p><strong>Tracked Users (entity signal):</strong> @connectedUsers.Count()</p>
  </div>
</div>

<div class="row">
  <div class="col">
    <strong>Users:</strong>
    <ul class="list-unstyled">
      @foreach (var user in connectedUsers)
      {
        <li>@user</li>
      }
    </ul>
  </div>
</div>
